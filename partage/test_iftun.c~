#include "iftun.h"


int main (void){

	char *dev2 = malloc(1024*sizeof(char));
	int src, dest;
	strcpy(dev2,"tun%d");
	dest = tun_alloc(dev2);
	
	if( (src = open("/dev/net/tun", O_RDWR)) < 0 )
  	{
				perror("création tun");
        exit(1);
  	}
	
	
	while(1){
		tun_copy(src, 1);
	}
	return 0;
}
